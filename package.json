{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "prepare": "husky install",
    "create-sym": "ln -s $(pwd)/public $(pwd)/build && ln -s $(pwd)/.env $(pwd)/build && ln -s $(pwd)/files $(pwd)/build",
    "commit": "git-cz"
  },
  "dependencies": {
    "@adonisjs/mrm-preset": "^5.0.3",
    "next": "^13.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "8.26.0",
    "eslint-config-next": "13.0.0",
    "husky": "^8.0.0"
  }
}
